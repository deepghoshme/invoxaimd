node_modules/
.env
.env.local

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# misc
.DS_Store

# lcdesign extracted working artifacts (keep the zip + .dc.html design refs only)
lcdesign/uploads/
lcdesign/.thumbnail
lcdesign/scraps/
lcdesign/*.js

# Live QA run artifacts (screenshots + results)
.qa-out/
